<%= form_tag authorizations_path, style: 'display: inline', class: action do %>
  <%= hidden_field_tag :client_id, client.identifier %>
  <%= hidden_field_tag :response_type, response_type %>
  <%= hidden_field_tag :scope, scopes.collect(&:name).join(' ') %>
  <%= hidden_field_tag :redirect_uri, redirect_uri %>
  <%= hidden_field_tag :state, state %>
  <%= hidden_field_tag :nonce, nonce %>
  <%= hidden_field_tag :request, _request_ %>
  <%= hidden_field_tag :request_uri, request_uri %>
  <% if action == :approve %>
  	<%= submit_tag "Approve".t, class: 'btn btn-success btn-lg' %>
  <% else %>
  	<%= submit_tag "Deny".t, class: 'btn btn-danger btn-lg' %>
  <% end %>
  <%= hidden_field_tag action, true %>
<% end %>